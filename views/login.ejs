<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="stylesheet" href="css/login.css" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %></title>
  </head>
  <body>
    <div class="main">
      <p class="sign" align="center">Sign in</p>
      <hr />
      <!-- register message for success -->
      <% if(registerStatus === 'true') { %>
      <p align="center" class="rMsg"><%= registerMsg %></p>
      <% } %>
      <!-- login message for fails -->
      <% if(loginStatus === 'false') { %>
      <p align="center" class="errorMsg"><%= loginMsg %></p>
      <% } %>

      <form class="form1" method="POST" action="/home">
        <input
          class="un"
          type="text"
          name="username"
          align="center"
          placeholder="Username"
          required="required"
        />
        <input
          class="pass"
          type="password"
          name="password"
          align="center"
          placeholder="Password"
          required="required"
        />
        <button type="submit" class="submit" align="center">Sign in</button>
        <a class="forgot" href="/register" align="center">Register</a>
      </form>
    </div>
  </body>
</html>
